- name: Start MariaDB
  shell: |
    cd {{ project_dir }}
    docker compose up -d mariadb

- name: Wait for MariaDB container is up
  include_tasks: "../common/tasks/wait_for_container.yml"
  vars:
    container_name: mariadb
